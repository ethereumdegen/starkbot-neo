{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the local engine-drift spike.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
